## What changed

Describe the focused change and why it is needed.

## Validation

- [ ] `npm ci`
- [ ] `npm run verify:versions`
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `cargo check` in `src-tauri`
- [ ] Windows behavior was manually checked when UI, WebView2, or updater behavior changed

## Risk

List affected operator flows, persistence, Google session behavior, window handling, and update behavior. Attach screenshots for visible UI changes and confirm that no secrets or session data are included.
